[package]
name = "ds-rust-football"
version = "0.1.0"
edition = "2024"

[dependencies]
polars = { version = "0.46", features = [
    "csv",
    "lazy",
    "strings",
    "dtype-struct",
] }
plotly = { version = "0.10", features = ["plotly_ndarray"] }
serde_json = "1"
